#include "val.h"

//#include <stdexcept>
/*
template <typename T>
val<T>::val() {
	calc = false;
}

template <typename T>
bool val<T>::isCalc() {
	return calc;
}

template <typename T>
T val<T>::getVal() {
	if(!calc)
		throw std::logic_error("La valeur n'a pas été calculé");
	else
		return _val;
}

template <typename T>
void val<T>::setVal(const T newVal) {
	calc = true;
	_val = newVal;
}*/
